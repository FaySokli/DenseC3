init:
  doc_model: 'nreimers/MiniLM-L6-H384-uncased'
  tokenizer: ${model.init.doc_model}
  device: 'cuda'
  aggregation_mode: 'mean'
  embedding_size: 384
  save_model: 'minilm'
  specialized_mode: 'weight'
  max_tokenizer_length: 256
  normalize: True
  temperature: 1
  num_experts_to_use: 4
  num_experts: 4

continue_train: False